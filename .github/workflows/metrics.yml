name: GitHub Metrics (Dark Theme & Multiple SVGs)

on:
  schedule:
    # Runs once every day at 6:00 AM IST
    - cron: "30 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      # ------------------------------------------
      # 1) BASE PROFILE SUMMARY (dark theme)
      # ------------------------------------------
      - name: Base Profile Summary
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics-base.svg
          config_timezone: Asia/Kolkata
          template: classic
          base: header, activity, community, repositories
          base_indepth: yes

          config_theme: dark

      # ------------------------------------------
      # 2) LANGUAGES (dark theme)
      # ------------------------------------------
      - name: Languages Breakdown
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics-languages.svg
          config_timezone: Asia/Kolkata

          template: classic
          config_theme: dark

          base: repositories
          plugin_languages: yes
          plugin_languages_ignored: html, css, scss
          plugin_languages_limit: 15
          plugin_languages_details: bytes-size, percentage
          plugin_languages_threshold: 2%
          plugin_languages_colors: github

      # ------------------------------------------
      # 3) ACTIVITY + CALENDAR (dark theme)
      # ------------------------------------------
      - name: Activity + Calendar
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics-activity.svg
          config_timezone: Asia/Kolkata

          template: classic
          config_theme: dark

          base: activity
          plugin_activity: yes
          plugin_activity_days: 30
          plugin_activity_filter: all
          plugin_activity_visibility: public

          plugin_calendar: yes
          plugin_calendar_limit: 1

      # ------------------------------------------
      # 4) ACHIEVEMENTS (dark theme)
      # ------------------------------------------
      - name: Achievements Overview
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics-achievements.svg
          config_timezone: Asia/Kolkata

          template: classic
          config_theme: dark

          base: ""
          plugin_achievements: yes
          plugin_achievements_threshold: X
          plugin_achievements_secrets: yes
          plugin_achievements_display: detailed
